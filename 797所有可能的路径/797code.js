/**
 * @param {number[][]} graph
 * @return {number[][]}
 */
var allPathsSourceTarget = function (graph) {
    var num = [], ans = [];
    console.log(num);
    for (var i = 0; i < graph.length; i++) {
        num.clear;
        num.push(i);
    }
};
allPathsSourceTarget([[1, 2], [3], [3], []])